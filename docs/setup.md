# Setup Guide
1. Install PostgreSQL and Python.
2. Run `sql/schema.sql` to create tables.
3. Configure `.env` with database credentials.
4. Run `scripts/ingest_data.py` to populate data.
5. Start the Flask app with `python app/app.py`.
